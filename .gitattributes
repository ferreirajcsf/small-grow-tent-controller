# Normalize all text files to LF in the repo, checked out as native on each OS.
* text=auto

# Force LF for Python and YAML — these must never have CRLF in production
*.py text eol=lf
*.yaml text eol=lf
*.yml text eol=lf
*.json text eol=lf
*.md text eol=lf

# Binary files — never touch line endings
*.png binary
*.jpg binary
*.ico binary
